version: '3.9'

services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-password}
      POSTGRES_DB: ${DB_NAME:-spicyragdb}
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./data:/docker-entrypoint-initdb.d
    command: >
      bash -c "apt-get update &&
               apt-get install -y postgresql-15-pgvector &&
               docker-entrypoint.sh postgres"

  interface_default:
    build: .
    command: >
      sh -c "
      until pg_isready -h db -p 5432; do
        echo 'Waiting for Postgres...'
        sleep 2
      done &&
      uvicorn interface.main:app --host 0.0.0.0 --port 8000 --reload"
    ports:
      - "8002:8000"
    environment:
      - OPENAI_API_KEY=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
    depends_on:
      - db
    volumes:
      - ./interface:/app/interface

volumes:
  postgres_data:
